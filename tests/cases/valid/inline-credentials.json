{
  "schema_version": 1,
  "id": "VALID-INLINE-CREDENTIALS",
  "title": "套件内测试凭证",
  "environment": {
    "name": "local",
    "db": {
      "host": "127.0.0.1",
      "database": "mtp_test",
      "password": "SuperSecret123"
    }
  },
  "secrets": {
    "ssh_password": "SshSecret123"
  },
  "steps": [
    {
      "id": "query",
      "action": "mysql.query",
      "args": {
        "credentials": "{{ env.db }}",
        "sql": "SELECT 1"
      }
    }
  ]
}
